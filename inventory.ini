[build_host]
# The RHEL 9.x or 10.x host that has image-builder installed and an active
# subscription. Use localhost if you run the playbook on the build host itself.
localhost ansible_connection=local

# Or a remote build host:
# rhel-builder.lab.local ansible_user=admin
